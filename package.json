{
  "name": "paper-atlas",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Paper Atlas — a dark, static citation-map site of ML/eval papers.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "cytoscape": "^3.28.1"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^4.1.10"
  }
}
